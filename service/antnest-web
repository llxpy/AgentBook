#!/sbin/openrc-run
# Alpine OpenRC 服务单元：antnest-web
# 安装后由 install.sh 执行 `rc-update add antnest-web default` 开机自启。
name="antnest-web"
description="AntNest Web · Linux 运维 Agent 控制服务（浏览器访问 :8080）"
command="/usr/bin/python3"
command_args="-u /opt/antnest-web/server.py"
command_background="yes"
pidfile="/run/${RC_SVCNAME}.pid"
output_log="/var/log/${RC_SVCNAME}.log"
error_log="/var/log/${RC_SVCNAME}.err"

depend() {
    need net
    after firewall
}
